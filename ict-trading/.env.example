# Copy to .env and fill in. .env is git ignored; never commit the real values.
# Token comes from the OANDA dashboard under Manage API Access.
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
# "practice" or "live". Everything up to gate 5 runs on practice.
OANDA_ENVIRONMENT=practice
